@model SettingsModel

<html>

<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css"
        integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <style>
        .blue {
            background-color: #123456 !important;
        }

        .grey {
            background-color: #ffcba4 !important;
        }

        .greyblue {
            background-color: #6789AB !important;
        }

        .rectangle {
            position: absolute;
            width: 100%;
            height: 35%;
        }

        .overlay {
            height: 40%;
            width: 100%;
        }

        .card-size {
            height: 300px;
            width: 10%;
        }

        .card {
            position: relative;
        }

        .padding-both-sides {
            padding-left: 25% !important;
            padding-right: 25% !important;
        }

        .padding-left {
            padding-left: 25% !important;
        }

        .form-size {
            width: 60%; !important
        }

        .form-padding {
            padding-left: 30px;
            padding-top: 30px;
            padding-right: 30px;
            display: none;
        }

        .form-result {
            width: 40%; !important
        }

        .float-left {
            float: left;
        }
    </style>
</head>
<body class="blue">
    <div class="blue">
        <div>
            <a id="none" asp-controller="Home" asp-action="Save" class="btn btn-light">Save</a>
            <a onclick="showhide('sortdiv')" class="btn btn-light">Sort</a>
            <a onclick="showhide('filterdiv')" class="btn btn-light">Filter</a>
            <a onclick="showhide('calculatediv')" class="btn btn-light">Calculate</a>
            <a id="none" asp-controller="Home" asp-action="ExpensesForm" class="btn btn-light">Create New
                Expense</a>
            <a id="none" asp-controller="Home" asp-action="ExpensesForm" asp-route-type="1" class="btn btn-light">Create
                New Income</a>
        </div>
        <div id="filterdiv" class="form-padding">
            <form asp-controller="Home" asp-action="Filter">
                <div>
                    <label class="form-label">Expenses</label>
                    <input asp-for=IsExpense type="checkbox" />
                    <label class="form-label padding-left">Incomes</label>
                    <input asp-for=IsIncome type="checkbox" />
                </div>
                <div class="form-row">
                    <div div class="col">
                        <label class="form-label">Date</label>
                        <input asp-for=From type="date" class="form-control" />
                    </div>
                    <div div class="col">
                        <label class="form-label">Date</label>
                        <input asp-for=To type="date" class="form-control" />
                    </div>
                </div>
                <div class="mt-3">
                    <button type="submit" class="btn btn-light">Submit</button>
                </div>
            </form>
        </div>
        <div id="sortdiv" class="form-padding">
            <form asp-controller="Home" asp-action="Sort" method="post">
                <div>
                    <label class="form-label">Sort by</label>
                    <select asp-for=SortType class="form-control">
                        <option id="Name" value = "Name" >Name</option>
                        <option id="Place" value="Place" >Place</option>
                        <option id="Amount" value = "Amount" >Amount</option>
                        <option id="Date" value="Date" >Date</option>
                    </select>
                </div>
                
                <div class="mt-3">
                    <button type="submit" class="btn btn-light">Submit</button>
                </div>
            </form>
        </div>
        <!--<div id="calculatediv" class="form-padding">
            <div class="form-size float-left">
                <form action="StatisticsSettings" method="post">
                    <div>
                        <label class="form-label ">Expense?</label>
                        <input asp-for=Statistics.IsExpense type="checkbox" />
                        <label class="form-label padding-left">Income?</label>
                        <input asp-for=Statistics.IsIncome type="checkbox" />
                    </div>
                    <div class="form-row">
                        <div div class="col">
                            <label class="form-label">Type</label>
                            <select asp-for=Statistics.Type class="form-control" id="type"
                                onchange="averageCheck(this);">
                                <option value="Total">Total</option>
                                <option value="Average">Average</option>
                            </select>
                        </div>
                        <div div class="col">
                            <label id="other" class="form-label">Period</label>
                            <select asp-for=Statistics.Period id="other" class="form-control">

                                <option value="1">Daily</option>
                                <option value="2">Monthly</option>
                                <option value="3">Yearly</option>

                            </select>
                        </div>
                        <div div class="col">
                            <label class="form-label">Date</label>
                            <input asp-for=Statistics.From type="date" class="form-control" />
                        </div>
                    </div>
                    <div class="mt-3">
                        <button type="submit" class="btn btn-light">Submit</button>
                    </div>

                </form>
            </div>
            <div class="float-left form-result">
                <div class="text-center">
                    <p class="">An app for finances</p>
                    </br>
                    <p class="">
                        
                    </p>
                </div>
            </div>
        </div>-->


        <br /><br />
        <table class="table text-bg-white">
            <thead>
                <tr class="text-white">
                    <th scope="col">Name</th>
                    <th scope="col">Category</th>
                    <th scope="col">Amount</th>
                    <th scope="col">Currency</th>
                    <th scope="col">Date</th>
                    <th scope="col">Place</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var expense in Model.PresentList) {@if(expense.isExpense==true){
                <tr class="bg-danger">
                    <td>@expense.Product</td>
                    <td>@expense.Category</td>
                    <td>@expense.Amount</td>
                    <td>@expense.Currency</td>
                    <td>@expense.Date.ToString("yyyy-MM-dd")</td>
                    <td>@expense.Place</td>
                    <td>
                        @Html.ActionLink(
                        "Edit",
                        "ExpensesForm", // controller action
                        "Home", // controller
                        new { id = expense.Index }, // action parameters aka route values
                        new { @class = "btn btn-light" })
                        @Html.ActionLink(
                        "Delete",
                        "ExpenseLineDelete", // controller action
                        "Home", // controller
                        new { id = expense.Index , type = 0}, // action parameters aka route values
                        new { @class = "btn btn-light" })

                    </td>
                </tr>
                }
                @if(expense.isExpense==false){
                <tr class="bg-success">
                    <td>@expense.Product</td>
                    <td>@expense.Category</td>
                    <td>@expense.Amount</td>
                    <td>@expense.Currency</td>
                    <td>@expense.Date.ToString("yyyy-MM-dd")</td>
                    <td>@expense.Place</td>
                    <td>
                        @Html.ActionLink(
                        "Edit",
                        "ExpensesForm", // controller action
                        "Home", // controller
                        new { id = expense.Index }, // action parameters aka route values
                        new { @class = "btn btn-light" })
                        @Html.ActionLink(
                        "Delete",
                        "ExpenseLineDelete", // controller action
                        "Home", // controller
                        new { id = expense.Index , type = 0}, // action parameters aka route values
                        new { @class = "btn btn-light" })

                    </td>
                </tr>
                }}
        </table>
    </div>
    <script>
        function showhide(buttonid) {
            if (buttonid != "sortdiv") {
                document.getElementById("sortdiv").style.display = "none";
            }
            /*if (buttonid != "calculatediv") {
                document.getElementById("calculatediv").style.display = "none";
            }*/
            if (buttonid != "filterdiv") {
                document.getElementById("filterdiv").style.display = "none";
            }

            if (document.getElementById(buttonid).style.display == "none") {
                document.getElementById(buttonid).style.display = "block";
            }
            else {
                document.getElementById(buttonid).style.display = "none";
            }
        }
    </script>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
        integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.14.3/dist/umd/popper.min.js"
        integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/js/bootstrap.min.js"
        integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy"
        crossorigin="anonymous"></script>
    <script src="https://www.kryogenix.org/code/browser/sorttable/sorttable.js"></script>

    <!--<script>
        document.getElementById('interface-output').onchange = function () {
            localStorage.setItem('selectedtem', document.getElementById('interface-output').value);
        };

        if (localStorage.getItem('selectedtem')) {
            document.getElementById('interface-output').options[localStorage.getItem('selectedtem')].selected = true;
        }
    </script>-->

</body>
</html>